import 'package:flutter/cupertino.dart';

class ControllerChart{
  ControllerChart();

  final ValueNotifier<bool> weekDummy = ValueNotifier(false);
  final ValueNotifier<bool> monthDummy = ValueNotifier(false);
}